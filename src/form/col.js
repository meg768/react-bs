import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';
import Container from '../container';

/**
 * 
 * @visibleName Form.Col
 * 
 */
function Col(props) {
    return (
        <Container.Col {...props}/>
    );
}

export default Col;